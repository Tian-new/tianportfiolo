# Project Notes

- This is a small static site. The primary source files are `index.html`, `styles.css`, and `script.js`.
- The `assets/` directory is large and media-heavy. Do not enumerate or inspect it unless a task specifically involves images, video, or asset paths.
- When checking the project, prefer targeted reads of the source files over broad file scans.
- If asset lookup is needed, inspect only the relevant subfolder.
